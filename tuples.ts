
/**
 * @aythor chandanch
 * Tuples are similar to array
 * we specify the types for the first two items of the array
 */

// creating tuples
let randomValues: [string, number] = ['Stack', 1000];

// adding more items to tuples it should have the same types as specified
randomValues[2] = 'Samules';
randomValues[3] = 34;
