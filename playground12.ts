/**
 *@description use of constants
 */
const interestRate = 4.5;
let balance = 4500;
let newBalance = balance + interestRate;
alert(`Your Balance: ${newBalance}`);
// Cannot change interestRate since its constant
//interestRate = 3.5
